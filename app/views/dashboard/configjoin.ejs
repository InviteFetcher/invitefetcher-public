<!DOCTYPE html>
<html>

<head>
    <title>InviteFetcher - Configure Welcome</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .navbar {
            background-color: #1F2B3D;
            color: #FFFFFF;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
        }

        .navbar-logo {
            color: #7289DA;
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
        }

        .navbar-button {
            background-color: #7289DA;
            color: #FFFFFF;
            border: none;
            padding: 5px 10px;
            font-weight: bold;
            text-decoration: none;
            border-radius: 5px;
        }

        .content {
            background-color: #1F2B3D;
            padding: 20px;
            text-align: center;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .config-cards {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        .config-card {
            background-color: #293949;
            width: 100%;
            max-width: 300px;
            border-radius: 5px;
            padding: 20px;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .config-card-title {
            font-size: 20px;
            font-weight: bold;
            color: white;
            margin-bottom: 10px;
        }

        .config-card-description {
            font-size: 16px;
            color: white;
            margin-bottom: 10px;
        }

        .config-card-description-warning {
            font-size: 14px;
            color: #FFA500;
            margin-bottom: 10px;
        }

        .config-card-description-warning-icon {
            margin-right: 5px;
        }

        .config-card-bar {
            display: flex;
            justify-content: center;
            margin-bottom: 10px;
        }

        .config-card-bar-option {
            background-color: #7289DA;
            color: white;
            padding: 5px 10px;
            border-radius: 5px;
            text-decoration: none;
            margin: 0 5px;
        }

        .config-card-content {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .config-card-content-title {
            font-size: 18px;
            font-weight: bold;
            color: white;
            margin-bottom: 10px;
        }

        .config-card-content-textarea {
            width: 100%;
            height: 100px;
            padding: 5px;
            margin-bottom: 10px;
            resize: none;
        }

        .config-card-content-button {
            background-color: #66BB6A;
            color: white;
            font-weight: bold;
            padding: 5px 10px;
            border-radius: 5px;
            text-decoration: none;
        }

        .footer {
            background-color: #1F2B3D;
            padding: 10px;
            color: #FFFFFF;
            text-align: center;
            font-size: 14px;
        }

        @media (max-width: 768px) {
            .config-card {
                max-width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="navbar">
        <a class="navbar-logo" href="/">InviteFetcher</a>
        <% if (isLoggedIn) { %>
            <a class="navbar-button" href="/dashboard">Dashboard</a>
            <% } else { %>
                <a class="navbar-button" href="/login">Login</a>
                <% } %>
    </div>

    <div class="content">
        <div class="config-cards">
            <div class="config-card config-card-black">
                <div class="error-card">
                    <h3 class="error-message">Arrival message</h3>
                </div>
                <form action="/dashboard/server/<%= serverId %>/welcome/save" method="POST">
                    <p class="config-card-description">The message sent by the bot when a user arrives on the server.
                    </p>
                    <p class="config-card-description-warning"><span
                            class="config-card-description-warning-icon">⚠️</span>If your invitation messages are not
                        sending, make sure the bot has permission to send messages in the configured room.</p>
                    <div class="config-card-bar">
                        <a class="config-card-bar-option" href="#">Normal arrival</a>
                        <a class="config-card-bar-option" href="#">Personalized invitation</a>
                    </div>
                    <div class="config-card-content">
                        <h3 class="config-card-content-title">Message content:</h3>
                        <textarea class="config-card-content-textarea" name="welcomeMessage" rows="4" cols="50"
                            placeholder="Enter the new welcome message"></textarea>
                        <label for="welcomeChannel">Choose a channel:</label>
                        <select name="welcomeChannel" id="welcomeChannel">
                            <% channels.forEach(function(channel) { %>
                                <option value="<%= channel.id %>">
                                    <%= channel.name %>
                                </option>
                                <% }); %>
                        </select>
                        <button class="config-card-content-button" type="submit">Save Changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <footer class="footer">
        <p>Made with ❤️ by cheraphdev</p>
    </footer>
</body>

</html>